import 'package:mvvm/data/local_db/UserDbModel.dart';

/*
class Helpers {

  static Future<int> insert(UserModelDb userModel) async {
    var store = await ObjectBoxStore.getStore();
    var box = store.box<UserModelDb>();
    return box.put(userModel);
  }

  static Future<bool> delete(int id) async {
    var store = await ObjectBoxStore.getStore();
    var box = store.box<UserModelDb>();
    return box.remove(id);
  }

  static Future<UserModelDb> queryPerson(int id) async {
    var store = await ObjectBoxStore.getStore();
    var box = store.box<UserModelDb>();
    return box.get(id);
  }
}*/
